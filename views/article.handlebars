<div class="row">
    <div class="card">
        <div class="card-header"><h3>{{article.title}}</h3>
        </div>
    <div class="card-body">{{body}}</div>
</div>

<div class="row">
    <div class="col"></div>
    <form action="/comment/{{article._id}}" method="POST"</form>
</div>